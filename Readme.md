Demonstration of apparent bug in tsx.

Type conformity is not enforced, or seemingly even checked.

Run:

`npx tsx bug.ts`

to see what I mean.

(you will probably need to run `npm install` first as well)